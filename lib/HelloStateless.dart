import 'package:flutter/material.dart';

class HelloStateless extends StatelessWidget {
  const HelloStateless({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
